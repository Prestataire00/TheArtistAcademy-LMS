// Onglet Ressources — liste des fichiers PDF/PPT classés par module
// Implémentation complète : Phase 1

export default function ResourcesPage({ params }: { params: { id: string } }) {
  return (
    <main className="max-w-4xl mx-auto p-6">
      <p className="text-gray-400">TODO: Phase 1 — ressources formation {params.id}</p>
    </main>
  );
}
