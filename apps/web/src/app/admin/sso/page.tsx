// Page diagnostic SSO — logs succès/échec, dernière synchro, config Dendreo
// Implémentation complète : Phase 0 (diagnostic) / Phase 2 (config complète)

export default function SsoDiagnosticPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-gray-900 mb-6">Diagnostic SSO Dendreo</h1>
      <p className="text-gray-400">TODO: Phase 0/2 — logs SSO et configuration</p>
    </div>
  );
}
